import 'package:flutter/cupertino.dart';

class AppMessage extends StatelessWidget {
  const AppMessage({
    super.key,
  });
  const AppMessage.myself({
    super.key,
  });
  const AppMessage.others({
    super.key,
  });
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
